[package]
name = "libAudio-rs"
version = "0.1.0"
description = "Rust bindings for libAudio"
license = "BSD-3-Clause"
authors = ["Rachel Mant <git@dragonmux.network>"]
edition = "2021"

[lib]
name = "libAudio"
path = "src/lib.rs"

[dependencies]
